// Copy this file to config.js (gitignored) and fill in the real key before
// running `npm run dist`. electron-builder bundles config.js into the app,
// so end users never see or enter a key themselves.
module.exports = {
  GROQ_API_KEY: 'gsk_...',
};
